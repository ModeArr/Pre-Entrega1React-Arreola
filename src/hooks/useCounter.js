/* import {useState} from 'react'

const useCounter = (intialValue = 0, step = 1 ) => {
    const [count, setCount] = useState(intialValue)

    const increment = () => {
        setCount(prevCount + step
    }
} */